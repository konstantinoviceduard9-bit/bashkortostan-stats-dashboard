# EMISS Source Agent

Специализация: **SDMX/XML** выгрузки Башкортостанстат / ЕМИСС.

## Коннектор

`EmissConnector` — требует `EMISS_SDMX_URL` в `.env`.

## Статус на дашборде

Если URL не задан → бейдж **«Не настроен»**.
